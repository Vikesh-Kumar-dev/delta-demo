# clone-> to be copied for respository ->git clone "link"
# status-> track changes in github -> git status
  -> stages of status => untracked->unmodified->modified ->staged -> commit
#add -> to add files with repository -> git add file name and git add . for all files
# commit -> to record changes -> final stamp -> git commit -m "meassage"
# git push origin main => used to push local dir to github account.
# git init => used to create new git repo.
# git remote add origin link => to push directory
# git remote -v => to verify the remote 
# git branch => to get branch
# git branch -M main => to rename branch



